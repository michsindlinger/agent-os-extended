appId: com.agent-os.manager
productName: Agent OS Manager
directories:
  buildResources: build
  output: dist-electron
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!electron.vite.config.ts'
  - '!{.eslintrc,.eslintrc.json,.eslintignore,.prettierrc,.prettierignore}'
  - '!{README.md,LICENSE.md,CHANGELOG.md}'
asarUnpack:
  - resources/**
mac:
  target:
    - target: dmg
      arch:
        - x64
  category: public.app-category.developer-tools
  identity: null
dmg:
  artifactName: ${productName}-${version}-${arch}.${ext}
win:
  target:
    - target: nsis
      arch:
        - x64
        - ia32
  icon: build/icon.ico
  artifactName: ${productName}-Setup-${version}.${ext}
nsis:
  oneClick: false
  perMachine: false
  allowToChangeInstallationDirectory: true
  deleteAppDataOnUninstall: false
linux:
  target:
    - target: AppImage
      arch:
        - x64
  icon: build/icon.png
  category: Development
  artifactName: ${productName}-${version}-${arch}.${ext}
